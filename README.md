# FF JODI
Mobile-first static prototype.

Open `index.html` locally or deploy this folder to GitHub Pages.
Put licensed/original images in `assets/guns/` and `assets/characters/`.
Authentication is intentionally only a frontend placeholder; connect Firebase/Auth before using real accounts.
